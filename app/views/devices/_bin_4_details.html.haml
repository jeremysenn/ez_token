- if (device.bill_count_4_exists? and (device.bill_count_4.status == 1 or device.bill_count_4.status == 2))
  %td{class: 'bg-danger disabled'}
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_4.status_description}"}
      = device.bin_4_count
      %br
      = number_to_currency(0, strip_insignificant_zeros: true)
    %p
      %small
        %strong Start
        = device.bill_count_4.host_start_count
        %br
        %strong Added
        = device.bill_count_4.added_count
        %br
        %strong Cycle
        = device.bill_count_4.host_cycle_count
- elsif (device.bill_count_4_exists? and device.bill_count_4.status == 4)
  %td{class: 'bg-warning disabled'}
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_4.status_description}"}
      = device.bin_4_count
      %br
      = number_to_currency(device.bin_4_total_amount)
    %p
      %small
        %strong Start
        = device.bill_count_4.host_start_count
        %br
        %strong Added
        = device.bill_count_4.added_count
        %br
        %strong Cycle
        = device.bill_count_4.host_cycle_count
- else
  -# unless device.bin_4_count.nil? or device.bin_4_denom.blank?
  - if device.bill_count_4_exists?
    %td{class: 'bin'}
      = device.bin_4_count
      %br
      = number_to_currency(device.bin_4_total_amount)
      %p
        %small
          %strong Start
          = device.bill_count_4.host_start_count
          %br
          %strong Added
          = device.bill_count_4.added_count
          %br
          %strong Cycle
          = device.bill_count_4.host_cycle_count

:javascript
  $('[data-toggle="tooltip"]').tooltip()