-# if @bin_1_column_count.nil? or @bin_1_column_count > 0
- if (device.bill_count_1.present? and device.bill_count_1.status == 1)
  %td{class: 'bg-danger disabled'}= link_to device.bin_1_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_1.status_description}"
- elsif (device.bill_count_1.present? and device.bill_count_1.status == 2)
  %td{class: 'bg-danger disabled'}= link_to device.bin_1_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_1.status_description}"
- elsif (device.bill_count_1.present? and device.bill_count_1.status == 4)
  %td{class: 'bg-warning disabled'}
    =# link_to device.bin_1_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_1.status_description}"
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_1.status_description}"}
      = device.bin_1_count
      %br
      = number_to_currency(device.bin_1_total_amount)
- else
  - unless device.bin_1_count.nil? or device.bin_1_denom.blank?
    %td{class: 'bin'}
      = device.bin_1_count
      %br
      = number_to_currency(device.bin_1_total_amount)

-# if @bin_2_column_count.nil? or @bin_2_column_count > 0
- if (device.bill_count_2.present? and device.bill_count_2.status == 1)
  %td{class: 'bg-danger disabled'}= link_to device.bin_2_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_2.status_description}"
- elsif (device.bill_count_2.present? and device.bill_count_2.status == 2)
  %td{class: 'bg-danger disabled'}= link_to device.bin_2_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_2.status_description}"
- elsif (device.bill_count_2.present? and device.bill_count_2.status == 4)
  %td{class: 'bg-warning disabled'}
    =# link_to device.bin_2_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_2.status_description}"
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_2.status_description}"}
      = device.bin_2_count
      %br
      = number_to_currency(device.bin_2_total_amount)
- else
  - unless device.bin_2_count.nil? or device.bin_2_denom.blank?
    %td{class: 'bin'}
      = device.bin_2_count
      %br
      = number_to_currency(device.bin_2_total_amount)

-# if @bin_3_column_count.nil? or @bin_3_column_count > 0
- if (device.bill_count_3.present? and device.bill_count_3.status == 1)
  %td{class: 'bg-danger disabled'}= link_to device.bin_3_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_3.status_description}"
- elsif (device.bill_count_3.present? and device.bill_count_3.status == 2)
  %td{class: 'bg-danger disabled'}= link_to device.bin_3_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_3.status_description}"
- elsif (device.bill_count_3.present? and device.bill_count_3.status == 4)
  %td{class: 'bg-warning disabled'}
    =# link_to device.bin_3_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_3.status_description}"
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_3.status_description}"}
      = device.bin_3_count
      %br
      = number_to_currency(device.bin_3_total_amount)
- else
  - unless device.bin_3_count.nil? or device.bin_3_denom.blank?
    %td{class: 'bin'}
      = device.bin_3_count
      %br
      = number_to_currency(device.bin_3_total_amount)

-# if @bin_4_column_count.nil? or @bin_4_column_count > 0
- if (device.bill_count_4.present? and device.bill_count_4.status == 1)
  %td{class: 'bg-danger disabled'}= link_to device.bin_4_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_4.status_description}"
- elsif (device.bill_count_4.present? and device.bill_count_4.status == 2)
  %td{class: 'bg-danger disabled'}= link_to device.bin_4_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_4.status_description}"
- elsif (device.bill_count_4.present? and device.bill_count_4.status == 4)
  %td{class: 'bg-warning disabled'}
    =# link_to device.bin_4_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_4.status_description}"
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_4.status_description}"}
      = device.bin_4_count
      %br
      = number_to_currency(device.bin_4_total_amount)
- else
  - unless device.bin_4_count.nil? or device.bin_4_denom.blank?
    %td{class: 'bin'}
      = device.bin_4_count
      %br
      = number_to_currency(device.bin_4_total_amount)

-# if @bin_5_column_count.nil? or @bin_5_column_count > 0
- if (device.bill_count_5.present? and device.bill_count_5.status == 1)
  %td{class: 'bg-danger disabled'}= link_to device.bin_5_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_5.status_description}"
- elsif (device.bill_count_5.present? and device.bill_count_5.status == 2)
  %td{class: 'bg-danger disabled'}= link_to device.bin_5_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_5.status_description}"
- elsif (device.bill_count_5.present? and device.bill_count_5.status == 4)
  %td{class: 'bg-warning disabled'}
    =# link_to device.bin_5_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_5.status_description}"
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_5.status_description}"}
      = device.bin_5_count
      %br
      = number_to_currency(device.bin_5_total_amount)
- else
  - unless device.bin_5_count.nil? or device.bin_5_denom.blank?
    %td{class: 'bin'}
      = device.bin_5_count
      %br
      = number_to_currency(device.bin_5_total_amount)

-# if @bin_6_column_count.nil? or @bin_6_column_count > 0
- if (device.bill_count_6.present? and device.bill_count_6.status == 1)
  %td{class: 'bg-danger-disabled'}= link_to device.bin_6_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_6.status_description}"
- elsif (device.bill_count_6.present? and device.bill_count_6.status == 2)
  %td{class: 'bg-danger-disabled'}= link_to device.bin_6_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_6.status_description}"
- elsif (device.bill_count_6.present? and device.bill_count_6.status == 4)
  %td{class: 'bg-warning disabled'}
    =# link_to device.bin_6_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_6.status_description}"
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_6.status_description}"}
      = device.bin_6_count
      %br
      = number_to_currency(device.bin_6_total_amount)
- else
  - unless device.bin_6_count.nil? or device.bin_6_denom.blank?
    %td{class: 'bin'}
      = device.bin_6_count
      %br
      = number_to_currency(device.bin_6_total_amount)

-# if @bin_7_column_count.nil? or @bin_7_column_count > 0
- if (device.bill_count_7.present? and device.bill_count_7.status == 1)
  %td{class: 'bg-danger disabled'}= link_to device.bin_7_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_7.status_description}"
- elsif (device.bill_count_7.present? and device.bill_count_7.status == 2)
  %td{class: 'bg-danger disabled'}= link_to device.bin_7_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_7.status_description}"
- elsif (device.bill_count_7.present? and device.bill_count_7.status == 4)
  %td{class: 'bg-warning disabled'}
    =# link_to device.bin_7_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_7.status_description}"
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_7.status_description}"}
      = device.bin_7_count
      %br
      = number_to_currency(device.bin_7_total_amount)
- else
  - unless device.bin_7_count.nil? or device.bin_7_denom.blank?
    %td{class: 'bin'}
      = device.bin_7_count
      %br
      = number_to_currency(device.bin_7_total_amount)

-# if @bin_8_column_count.nil? or @bin_8_column_count > 0
- if (device.bill_count_8.present? and device.bill_count_8.status == 1)
  %td{class: 'bg-danger disabled'}= link_to device.bin_8_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_8.status_description}"
- elsif (device.bill_count_8.present? and device.bill_count_8.status == 2)
  %td{class: 'bg-danger disabled'}= link_to device.bin_8_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_8.status_description}"
- elsif (device.bill_count_8.present? and device.bill_count_8.status == 4)
  %td{class: 'bg-warning disabled'}
    =# link_to device.bin_8_count, '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_8.status_description}"
    %a{href: '#', "data-toggle" => "tooltip", "data-placement" => "right", "title" => "#{device.bill_count_8.status_description}"}
      = device.bin_8_count
      %br
      = number_to_currency(device.bin_8_total_amount)
- else
  - unless device.bin_8_count.nil? or device.bin_8_denom.blank?
    %td{class: 'bin'}
      = device.bin_8_count
      %br
      = number_to_currency(device.bin_8_total_amount)

:javascript
  $('[data-toggle="tooltip"]').tooltip()