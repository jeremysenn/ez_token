= render 'shared/flash_messages'
.login-box
  .login-logo
    %a{:href => root_path}
      -# <strong>ez</strong>Cash
      = image_tag(image_path("ezcash_logo_50.png"))
  .login-box-body
    %p.login-box-msg Sign up
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = devise_error_messages!
      = f.hidden_field :company_id, value: current_user.blank? ? 7 : current_user.company_id
      =# f.hidden_field :role, value: current_user.blank? ? "consumer" : "basic"
      = f.hidden_field :role, value: "basic"
      .form-group
        = f.text_field :first_name, autofocus: true, autocomplete: "first_name", class: 'form-control', :placeholder => "First Name*", required: true
      .form-group
        = f.text_field :last_name, autocomplete: "last_name", class: 'form-control', :placeholder => "Last Name*", required: true
      .form-group
        = f.email_field :email, autocomplete: "email", class: 'form-control', :placeholder => "Email*", :type => "email", required: true
      .form-group
        = f.phone_field :phone, autocomplete: "phone", class: 'form-control', :placeholder => "Phone*", :type => "phone", required: true
      .form-group
        = f.password_field :password, autocomplete: "off",  class: 'form-control', :placeholder => "Password*", required: true
        - if @minimum_password_length
          %small#passwordHelpBlock.form-text.text-muted
            = "Your password must be at least #{@minimum_password_length} characters long."
      .form-group
        = f.password_field :password_confirmation, autocomplete: "off",  class: 'form-control', :placeholder => "Retype password*", required: true
      .form-group
        .checkbox.icheck
          %label
            %input{:type => "checkbox"}/
            I agree to the
            %a{:href => "#"} terms
      .actions
        = f.submit "Sign up", class: 'btn btn-primary btn-block'
      = link_to "Log in", new_user_session_path
    =# render "devise/shared/links"

%script
  $('#user_phone').mask("(999) 999-9999");